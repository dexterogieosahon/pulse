<%= form_for(Message.new) do |f| %>
  <div id="post" class="field">
    <%= f.text_area :content, placeholder: "Post new message..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>